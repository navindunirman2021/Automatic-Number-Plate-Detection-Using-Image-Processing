# Automatic-Number-Plate-Detection-Using-Image-Processing
Automatic number plate detection system implemented in Python and OpenCV. Uses a classical image processing pipeline (grayscale, CLAHE, bilateral filter, morphology, gradients, thresholding, ROI, contour analysis) to localize the license plate region and produce a cropped plate image for future OC
